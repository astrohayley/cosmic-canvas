{
  "name": "cosmic-canvas-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end Puppeteer tests for the Cosmic Canvas gh-pages site.",
  "scripts": {
    "test": "node --test --test-concurrency=1 e2e.test.mjs parity.test.mjs",
    "test:e2e": "node --test --test-concurrency=1 e2e.test.mjs",
    "test:parity": "node --test --test-concurrency=1 parity.test.mjs"
  },
  "devDependencies": {
    "puppeteer": "^24.0.0"
  }
}
